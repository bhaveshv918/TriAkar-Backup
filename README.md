# TriAkar v5

## Key fixes in this version
- Header overlap FIXED: body { padding-top: var(--total-h) } handles it globally
- TRIAKAR single word on homepage
- Hindi logo 30% larger (13px, was 11px)
- Razorpay LIVE key: rzp_live_SQfXl2pEG4hZki
- PLA+ as default material everywhere
- Mobile sticky buy bar on product pages
- Mobile filter bar horizontal scroll on products page
- Print animation: real FDM object silhouette (vase/organizer shape)
- Category grid mobile optimised

## Deploy
```bash
git init && git add . && git commit -m "TriAkar v5"
git branch -M main
git remote add origin https://github.com/USERNAME/triakar.git
git push -u origin main
```
Settings > Pages > Source: main, root.
